{
  "name": "personal-expense-tracker-backend",
  "version": "1.0.0",
  "description": "Backend for Personal Expense Tracker",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "deploy": "serverless deploy",
    "offline": "serverless offline",
    "invoke-local": "serverless invoke local --function createExpense"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.26.0",
    "@aws-sdk/lib-dynamodb": "^3.26.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "serverless": "^2.72.0"
  }
}
